import Link from 'next/link'
import { ArrowLeft, CircleCheck as CheckCircle2 } from 'lucide-react'
import { modelWeights, disclaimer, riskThresholds } from '@/lib/climate/types'
export default function Methodology(){return <main className="min-h-screen bg-[#f6f7f2] px-5 py-10 text-[#18332b] lg:px-8"><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="text-sm text-[#5e776b]"><ArrowLeft className="mr-2 inline size-4"/>Back to dashboard</Link><p className="mt-16 text-xs font-semibold uppercase tracking-[.16em] text-[#668a79]">Methodology</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.07em]">A transparent model,<br/>not a crystal ball.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-[#71887d]">TerraShield translates scenario assumptions into explainable modeled risk scores. It is designed for climate awareness and decision support—not official warnings.</p><div className="mt-12 grid gap-5 md:grid-cols-2"><section className="rounded-2xl bg-white p-6"><h2 className="font-semibold">Flood model</h2><p className="mt-4 rounded-xl bg-[#edf4ea] p-4 font-mono text-sm">risk = rainfall × {modelWeights.flood.rainfall} + terrain × {modelWeights.flood.terrain} + drainage × {modelWeights.flood.drainage}</p><p className="mt-4 text-sm leading-6 text-[#71887d]">Inputs are normalized to a 0–1 range, weighted, then converted to a 0–100 score.</p></section><section className="rounded-2xl bg-[#18332b] p-6 text-white"><h2 className="font-semibold">Heat model</h2><p className="mt-4 rounded-xl bg-white/10 p-4 font-mono text-sm">risk = temp × .55 + built-up × .25 + vegetation × .20</p><p className="mt-4 text-sm leading-6 text-[#b7cec0]">Lower vegetation coverage contributes to vulnerability in the prototype scenario.</p></section></div><section className="mt-5 rounded-2xl border border-[#dbe4dc] bg-white p-6"><h2 className="font-semibold">Prototype risk categories</h2><p className="mt-2 text-xs text-[#80958b]">These are illustrative prototype thresholds, not official risk classification standards.</p><div className="mt-5 grid gap-3 sm:grid-cols-4">{riskThresholds.map(item=><div key={item.label} className="rounded-xl bg-[#f1f5ef] p-4"><div className="text-xs text-[#71887d]">{item.range}</div><div className="mt-2 text-sm font-semibold">{item.label}</div></div>)}</div></section><section className="mt-5 rounded-2xl border border-[#e4d3a8] bg-[#fff9e9] p-6"><h2 className="font-semibold">Limitations</h2><div className="mt-4 grid gap-3 text-sm text-[#6e6244]">{['Prototype weighting and scenario assumptions','Synthetic demonstration data','No operational validation or official alerting','Scores should not replace local authority guidance'].map(item=><div key={item}><CheckCircle2 className="mr-2 inline size-4"/>{item}</div>)}</div><p className="mt-5 text-xs leading-5 text-[#897b5a]">{disclaimer}</p></section></div></main>}
